using UnityEngine;

public class MainMenuCrowReaction : MonoBehaviour
{
    // Empty implementation so the crow only has its idle and does not react to the menu or lightning.
    public void ReactToHover()
    {
    }

    public void ReactToLightning()
    {
    }
}
